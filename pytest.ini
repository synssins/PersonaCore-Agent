[pytest]
pythonpath = src
asyncio_mode = auto
addopts = --strict-markers --timeout=120 --timeout-method=thread
norecursedirs = worktree .venv .git __pycache__ node_modules dist build _incoming
timeout = 120
